<p id="notice"><%= notice %></p>

<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <td><b>Profile Picture</b></td>
      <td><b>Nickname</b></td>
      <% if current_user %>
        <td><b>Full Name</b></td>
        <td><b>Age</b></td>
        <td><b>Gender</b></td>
      <% end %>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <tr>
        <td><%= image_tag @user.avatar_thumbnail %></td>
        <td><%= @user.nickname %></td>
        <% if current_user %>
            <td><%= @user.full_name %></td>
            <td><%= @user.birth_date %></td>
            <td><%= @user.gender %></td>
        <% end %>
    </tr>
  </tbody>
</table>

<%= link_to 'Back', users_path %>
